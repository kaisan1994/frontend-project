const API = {
  route: '/route',
};

const statusCode = {
  success: 200,
  serverError: 500,
  badRequest: 400,
};

const responseStatus = {
  inProgress: 'in progress',
  failure: 'failure',
  success: 'success',
};

export { API, responseStatus, statusCode };
